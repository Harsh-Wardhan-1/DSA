#include <iostream>
using namespace std;
void min_heapify(int arr, int m, int n)
{
    int j = 2 * n, t,
        t = arr[n];
    while (j <= n)
    {
        if (j < n && arr[j + 1] < arr[j])
            j if (t < arr[j])
            {

                t = arr[a];

                j = 2 * m;
                while (jn)

                    break;
                else if (t > arr[j])

                        arrl1 /
                    21 = arr[j];
                j = 2.1;

arrij / 2] = t;
return;

void build ninheaplint warr, int n)

int k;
for (k = n / 2; k > 1; k -)

    min heapify(arr, k, n) :

                             int main() int main()
    {

        int n, i, k;
        int arr[100];

        cout << "Enter total elements" << endl;

        cin >> n;
        cout << "Enter the kth element" << endl;

        cin >> k;

        cout << "enter array" << endl;
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
    }
build_minheap(arr, n);
for (int i = 0; i < n; i++)
{
    cout << arr[i] << " ";
}
cout << "the element is : " << arr[k - 1];

return 0;

cin >> arr[i];

nt)

)